diff --git a/figure1.py b/figure1.py
index d7f5e95..8b2462e 100644
--- a/figure1.py
+++ b/figure1.py
@@ -500,6 +500,19 @@ def plot_left_line_plot(
     flexitext(LAYOUT["suptitle_x"],0.715, title_a_up, va="bottom", xycoords="figure fraction")
     flexitext(LAYOUT["suptitle_x"],0.69, title_a_down, va="bottom", xycoords="figure fraction")
 
+    # extend grid lines from middle line to left bar chart
+    for y in ax.get_yticks():
+        ax.plot(
+            [-430, -230],  # extends left of xlim(1) into the left subplot area
+            [y, y],
+            color=axis_color if y != 0 else black_color,
+            linestyle=":",
+            linewidth=1,
+            alpha=1 if y == 0 else 0.5,
+            zorder=0,
+            clip_on=False  # allow drawing outside axis bounds
+        )
+
 
 def plot_right_line_plot(
     ax,
