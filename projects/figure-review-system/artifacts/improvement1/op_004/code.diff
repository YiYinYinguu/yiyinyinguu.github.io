diff --git a/figure1.py b/figure1.py
index 926e895..1e3535d 100644
--- a/figure1.py
+++ b/figure1.py
@@ -86,7 +86,7 @@ TEXTS = {
         + "<name: Arial, size: 11>factors (both 1850-2020)</>"
     ),
     "annotations_a": [
-        {"gid": "a.anno.warmest",       "xy": (1,   1.1), "text": "Warmest multi-century\nperiod in more than\n100,000 years"},
+        {"gid": "a.anno.warmest",       "xy": (-400, 1.1), "text": "Warmest\nmulti-century\nperiod"},
         {"gid": "a.anno.unprecedented", "xy": (700, 1.6), "text": "Warming is unprecedented\nin more than 2000 years"},
         {"gid": "a.anno.observed",      "xy": (1550, 0.8), "text": "observed"},
         {"gid": "a.anno.reconstructed", "xy": (700, -0.4), "text": "reconstructed"},
@@ -325,18 +325,7 @@ def plot_left_bar_chart(ax):
     ax.spines["bottom"].set_visible(False)
 
     # Add annotation with arrow
-    tag(ax.annotate(
-        " ",
-        xy=(1, 1.1),
-        xytext=(2.5, 1.3),
-        arrowprops=dict(
-            arrowstyle="->",
-            connectionstyle="arc3,rad=0.3",  # Curvature of the arrow
-            color=axis_color,
-            linewidth=1,
-        ),
-        clip_on=False,
-    ), "bar.arrow")
+    # bar.arrow removed
     ax.text(
         -0.5,
         -1.35,
@@ -457,7 +446,8 @@ def plot_left_line_plot(
         TEXTS["annotations_a"][0]["xy"][1],
         TEXTS["annotations_a"][0]["text"],
         fontsize=FONTS["body"],
-        color=black_color,
+        color=warm_color,
+        horizontalalignment="center",
     ), TEXTS["annotations_a"][0]["gid"])
     tag(ax.text(
         TEXTS["annotations_a"][1]["xy"][0],
