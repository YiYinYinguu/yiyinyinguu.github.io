diff --git a/figure1.py b/figure1.py
index 34083c9..03a1471 100644
--- a/figure1.py
+++ b/figure1.py
@@ -404,6 +404,17 @@ def plot_left_line_plot(
         clip_on=False,
     ), "a.arrow")
 
+    # Bracket under the 1850-2020 span marking the region panel (b) zooms into.
+    _zx0, _zx1 = AXES["span"]
+    _zy = -1.22
+    ax.plot([_zx0, _zx1], [_zy, _zy], color=axis_color, lw=1, clip_on=False, zorder=5)
+    ax.plot([_zx0, _zx0], [_zy, _zy + 0.05], color=axis_color, lw=1, clip_on=False, zorder=5)
+    ax.plot([_zx1, _zx1], [_zy, _zy + 0.05], color=axis_color, lw=1, clip_on=False, zorder=5)
+    tag(ax.text(
+        (_zx0 + _zx1) / 2, _zy - 0.06, "Zoomed in on the right",
+        ha="center", va="top", fontsize=8.5, color=label_color, clip_on=False,
+    ), "a.anno.zoom")
+
     flexitext(LAYOUT["suptitle_x"],0.715, title_a_up, va="bottom", xycoords="figure fraction")
     flexitext(LAYOUT["suptitle_x"],0.69, title_a_down, va="bottom", xycoords="figure fraction")
 
@@ -630,6 +641,16 @@ def build_figure():
         transform=fig.transFigure,
         fontweight="bold",
     )
+    tag(plt.text(
+        LAYOUT["panel_b_x"],
+        0.663,
+        "Zoom-in: 1850-2020",
+        fontsize=10,
+        color=AXES["label"],
+        ha="left",
+        style="italic",
+        transform=fig.transFigure,
+    ), "b.anno.zoom")
     plt.subplots_adjust(left=LAYOUT["left"], bottom=LAYOUT["bottom"], top=LAYOUT["top"])
     return fig
 
